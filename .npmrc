# krgeobuk-infrastructure 중앙 Verdaccio 레지스트리 사용
registry=http://localhost:4873

# 중앙 Verdaccio는 krgeobuk-infrastructure/docker-compose에서 관리됩니다
# 시작: cd ../krgeobuk-infrastructure/docker-compose && docker-compose up -d
# 중지: cd ../krgeobuk-infrastructure/docker-compose && docker-compose down

# 사용자 등록
# pnpm adduser --registry http://localhost:4873

# 패키지 게시
# pnpm publish --no-git-checks --registry http://localhost:4873

# 패키지 설치
# npm install @krgeobuk/database-config --registry=http://localhost:4873