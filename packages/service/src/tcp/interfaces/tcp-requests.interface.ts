/**
 * User 도메인 TCP 파라미터 인터페이스
 * 사용자 관련 TCP 통신에서 사용되는 파라미터
 */

import type { ServiceIdParams } from '@krgeobuk/shared/service';

// shared의 기본 ID params 재사용
export type TcpServiceId = ServiceIdParams;
